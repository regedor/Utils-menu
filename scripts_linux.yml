scripts:
    - script:
        name: Copy(replace) vim files.
        description: ...
        path: linux1.sh
    - script:
        name: Copy(replace) all HOME files.
        description: ...
        path: linux2.sh
    - script:
        name: Install Ruby on Rails (and dependencies).
        description: ...
        path: linux3.sh
    - script:
        name: Edit bashrc file.
        description: ...
        path: linux4.sh
    - script:
        name: Teste sub-menu
        description: ...
        scripts:
            - script:
                name: Sub cenas 1
                description: ...
                path: non_exist1
            - script:
                name: Sub cenas 2
                description: ...
                path: non_exist2
